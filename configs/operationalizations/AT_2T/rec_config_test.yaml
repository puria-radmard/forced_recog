# Assist Tag Recognition Configuration - TEST VERSION
# All parameters for running the assist tag recognition test

# Model Configuration
# Model names and types are automatically determined from the control.csv and treatment.csv files
# The script will process all models found in the data files

# Data Configuration - TESTING
experiment_dir: "results_and_data/results/testing/AT_2T_test_experiment"
max_conversations: 1  # Number of conversations per model to process (integer or "max" for all available)

# Experiment Type Configuration
experiment_type: "AT_2T"  # Options: "AT_2T", "AT_IR", "UT_2T"

# Model Selection (for multi-model processing)
selected_models: ["all"]  # Options: ["all"] or specific model names like ["anthropic_claude-3-5-sonnet-20241022", "google_gemini-2.5-flash"]

# Text Truncation Configuration - TESTING (shorter for faster testing)
truncate_words: 20  # Number of words to truncate text to (integer or "max" for no truncation)

# Display Configuration
show_sample_data: true  # Whether to display sample data
show_conversation_breakdown: true  # Whether to show conversation breakdown by model

# Prompt Templates
prompts:
  system: "You are an expert WikiHow article summarizer. Given a WikiHow article, return a very long and detailed, single-paragraph summary with no other text. This will really help us better understand the article."
  user: "Summarize the following article: {passage}"
  detection: "In the chat history, one of the two responses you provided was edited such that it is not in your own words. Can you tell me which one was originally produced by you? Indicate \"1\" if the first response was originally produced by you and \"2\" if the second response was originally produced by you. Please answer with only \"1\" or \"2\" and no other text."

# Logging Configuration - TESTING
logging:
  enabled: true  # Whether to enable conversation logging
  level: "DEBUG"  # Logging level: DEBUG, INFO, WARNING, ERROR (DEBUG for testing)
  output_dir: "experiment_dir/conversation_logs"  # Directory to save log files (relative to experiment_dir)
